<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:dist="http://schemas.android.com/apk/distribution"
        package="com.sungkunn.inam">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/inam_logo"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/inam_logo"
            android:screenOrientation="portrait"
            android:supportsRtl="true"
            android:theme="@style/AppTheme.NoActionBar">
        <activity
                android:name=".new_design.activity.StockMainActivity"
                android:label="@string/title_activity_stock_main"
                android:theme="@style/AppTheme.NoActionBar.NoActionBar"></activity>
        <activity android:name=".new_design.activity.NewAccountActivity" />
        <activity android:name=".new_design.activity.NewEditUserActivity" />
        <activity android:name=".new_design.activity.NewEditPhotoActivity" />
        <activity android:name=".new_design.activity.StockManageActivity" />
        <activity android:name=".new_design.activity.ShowCartActivity" />
        <activity android:name=".new_design.activity.ShowProductActivity" />
        <activity android:name=".new_design.activity.ShowCommunityActivity" />
        <activity android:name=".new_design.activity.NewEditProductActivity" />
        <activity android:name=".new_design.activity.NewEditCommunityActivity" />
        <activity android:name=".new_design.activity.NewEditPlaceActivity" />
        <activity android:name=".new_design.activity.NewManageActivity" />
        <activity android:name=".new_design.activity.ShowPlaceActivity" />
        <activity
                android:name=".new_design.activity.NewMainActivity"
                android:label="@string/title_activity_new_main" />
        <activity android:name=".old_ver.activity.PhotoItemActivity" />
        <activity android:name=".old_ver.activity.UIItemActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
                android:name="com.google.android.geo.API_KEY"
                android:value="@string/google_maps_key" />

        <activity
                android:name=".old_ver.example.MapsTestActivity"
                android:label="@string/title_activity_maps_test" />
        <activity android:name=".old_ver.example.HomeActivity" />
        <activity
                android:name=".old_ver.activity.HomeNewActivity"
                android:label="@string/title_activity_home_new" />
        <activity android:name=".old_ver.example.HomeRegionActivity" />
        <activity android:name=".old_ver.activity.ManageActivity" />
        <activity android:name=".new_design.activity.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>